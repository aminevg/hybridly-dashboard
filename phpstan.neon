includes:
    - ./vendor/nunomaduro/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    paths:
        - app/
        - routes/
        - database/
        - tests/
        - lang/

    level: max

    scanFiles:
        - _ide_helper_models.php
